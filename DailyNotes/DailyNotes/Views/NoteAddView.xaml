<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DailyNotes.Views.NoteAddView"
             xmlns:local="clr-namespace:DailyNotes.ViewModels"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms" 
             prism:ViewModelLocator.AutowireViewModel="True"
             >
  <ContentPage.Content>
        <StackLayout Margin="20"
                   VerticalOptions="StartAndExpand">
            <Label Text="名前" TextColor="Black"/>
            <Entry Text="{Binding Name.Value}" TextColor="Black"/>
            <Label Text="本文" TextColor="Black"/>
            <Entry Text="{Binding Contents.Value}" TextColor="Black"/>
            <Label Text="日時" TextColor="Black"/>
            <DatePicker Date="{Binding Input.Value}" TextColor="Black"/>
            <Label Text="Done" TextColor="Black"/>
            <Switch IsToggled="{Binding IsDone.Value}"/>

            <Button Text="登録"
                    Command="{Binding NoteSubmitCommand, Mode=TwoWay}"/>
            <Button Text="削除"
                    Command="{Binding NoteDeleteCommand}"/>
            <Button Text="キャンセル"
                    Command="{Binding CancelCommand}"/>
        </StackLayout>
  </ContentPage.Content>
</ContentPage>